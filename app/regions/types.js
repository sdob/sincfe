import { createTypes } from '../shared';

const regionClubList = createTypes('REGION_CLUB_LIST');
const regionDetail = createTypes('REGION_DETAIL');
const regionList = createTypes('REGION_LIST');
const regionMemberList = createTypes('REGION_MEMBER_LIST');

export {
  regionClubList,
  regionDetail,
  regionList,
  regionMemberList,
};
